# TODO

- [ ] Collect Vercel build log fatal error text (first `ERROR`/`failed to load config`/`TSxxxx`).
- [ ] Fix local dependency install/build issues:
  - [ ] Run `npm ci` with a clean reinstall after stopping VSCode/antivirus locks.
  - [ ] Ensure `node_modules/vite` exists.
- [ ] After local build succeeds, re-run Vercel deploy and confirm build passes.

