const AdminReports = () => {
  // Reports page intentionally removed from admin dashboard.
  // Kept as a minimal component to prevent broken imports during development.
  return null;
};

export default AdminReports;

